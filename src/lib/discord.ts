import { EmbedBuilder } from 'discord.js';

export const ErrorEmbed = new EmbedBuilder().setTitle('Error').setColor('DarkRed');

export const SuccessEmbed = new EmbedBuilder().setTitle('Success').setColor('DarkGreen');
